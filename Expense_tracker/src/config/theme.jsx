import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2779a7",
      contrastText: "#E3FDFD",
    },
    secondary: {
      main: "#A6E3E9",
    },
  },
});
export default theme;
