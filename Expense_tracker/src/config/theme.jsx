import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#00ADB5",
      contrastText: "#E3FDFD",
    },
    secondary: {
      main: "#A6E3E9",
    },
  },
});
export default theme;
